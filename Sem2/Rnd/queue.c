#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
typedef struct
{
    int bottom;
    int top;
    int size;
    int *items;
}queue;

int main(){
queue mint;
mint.items=(int*)malloc(sizeof(int)*mint.size);

return 0;
}